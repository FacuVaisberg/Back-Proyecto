import 'dotenv/config'

const config = {
    server : "localhost",
    user : "Pharmalife",
    pasword: "paton123",
    database: "PharmaLife",
    options : {
        trustServerCertificate : true,
        trustConnection : true
    }
}
export default config;